# Derivative Workflows

## Image summary

```bash
python3 skills/archlens/scripts/agent.py image-summary item <itemKey>
```

Requires an existing `summaryCard.json`.

## Mindmap

```bash
python3 skills/archlens/scripts/agent.py mindmap item <itemKey>
```

Requires an existing `summaryCard.json`.
